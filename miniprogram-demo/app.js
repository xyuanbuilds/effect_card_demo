App({
  onLaunch() {
    // Lifecycle function -- Listen to mini program initialization
  },
  onShow() {
    // Lifecycle function -- Listen to mini program show
  },
  onHide() {
    // Lifecycle function -- Listen to mini program hide
  },
  onError(msg) {
    console.log(msg);
  },
  globalData: {
    userInfo: null,
  },
});
